<%= simple_form_for [user, food_picture], remote: true, authenticity_token: true do |f| %>
  <%= f.input :picture do %>
    <i class="fa fa-upload"></i>
  <% end %>
  <%= f.button :submit, "Enregistrer", class: "btn btn-primary" %>
<% end %>
