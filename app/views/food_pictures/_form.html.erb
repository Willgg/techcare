<%= simple_form_for [user, food_picture], authenticity_token: true do |f| %>
  <%= f.input :picture, label: t('.food_add') %>
  <%= f.button :submit, t('.food_submit'), class: 'btn-techcare', id: 'food_picture_submit' %>
<% end %>
