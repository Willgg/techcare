<div class="message">
 <span><small><%=message.created_at.strftime("%H:%M le %d/%m/%y")%></small></span>
<p><%= image_tag message.sender.picture.url(:thumb) %>
  <strong><%=message.sender.first_name%></strong>
</p>
<p><%=message.content%></p>
</div>
