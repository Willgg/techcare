$('.messages').append("<%= j render 'messages/message', message: @message %>");
$('.messages')[0].scrollTop = $('.messages')[0].scrollHeight
$('.message').last().addClass('fadeInUp animated');
$('.chat-card textarea').val('');
